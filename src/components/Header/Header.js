import React from 'react';
import { AppBar } from 'material-ui';

export const Header = () => (
    <AppBar
      iconElementLeft={<div />}
      title="Customers Manager" />
);

export default Header;
